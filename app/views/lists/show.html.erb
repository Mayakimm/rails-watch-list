<%= @list.name %>
<% @list.bookmarks.each do |bookmark| %>
<%= bookmark.comment %>
<%= link_to "Delete a bookmark", list_bookmark_path(@list, bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<% end %>
<%= link_to "Add a bookmark", new_list_bookmark_path(@list) %>
